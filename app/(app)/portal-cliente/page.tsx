import { requireModule } from "@/lib/auth";import { redirect } from "next/navigation";import { prisma } from "@/lib/prisma";import { money,dateOnly } from "@/lib/format";export default async function Page(){const g= await requireModule("portal-cliente");if(!g.ok)redirect("/home");const client=await prisma.client.findFirst({include:{projects:{include:{stages:true,tasks:true}},invoices:true,quotes:{include:{items:true}},contracts:true}});return <div className="content-card"><div className="topbar"><div><h1 className="section-title">Portal Cliente</h1><p className="muted">Propuestas, contratos y avances.</p></div><span className="pill">{client?.commercialName}</span></div><section className="grid three"><div className="card"><h2>Propuestas</h2><h1>{client?.quotes.length}</h1></div><div className="card"><h2>Contratos</h2><h1>{client?.contracts.length}</h1></div><div className="card"><h2>Proyectos</h2><h1>{client?.projects.length}</h1></div></section><div className="card"><h2>Propuestas para aceptar</h2>{client?.quotes.map(q=><div className="task-card" key={q.id}><h3>{q.number}</h3><p>{q.items.map(i=>i.description).join(", ")}</p><h2>{money(q.total)}</h2><span className="badge">{q.status}</span>{q.status!=="ACEPTADO"?<form action="/api/quotes/accept" method="POST"><input type="hidden" name="quoteId" value={q.id}/><button>Aceptar propuesta</button></form>:null}</div>)}</div><div className="card"><h2>Contratos</h2>{client?.contracts.map(c=><div className="task-card" key={c.id}><strong>{c.name}</strong><p>{dateOnly(c.startDate)} - {dateOnly(c.endDate)}</p>{c.contractPdfUrl?<a className="btn secondary" href={c.contractPdfUrl} target="_blank">Ver contrato PDF</a>:<span className="muted">Sin PDF cargado</span>}</div>)}</div><div className="card"><h2>Avance de proyectos</h2>{client?.projects.map(p=><div className="task-card" key={p.id}><h3>{p.name}</h3>{p.tasks.map(t=><p key={t.id}>{t.title} · {t.progress}% · {t.status}</p>)}</div>)}</div><div className="card"><h2>Facturas</h2><table className="table"><tbody>{client?.invoices.map(i=><tr key={i.id}><td>{i.number}</td><td>{dateOnly(i.dueDate)}</td><td>{i.status}</td><td>{money(i.amount)}</td></tr>)}</tbody></table></div></div>}



